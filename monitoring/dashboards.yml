apiVersion: 1

providers:
  - name: "default"
    folder: "Monitoring"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
